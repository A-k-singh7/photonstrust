"""Layout utilities (v0).

This package intentionally starts with *route-level* (pre-GDS) geometry helpers.
The first use-case is extracting simple features (e.g., parallel run segments)
to connect performance DRC checks to deterministic routing metadata.
"""

