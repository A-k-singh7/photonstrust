"""Evidence bundles and trust artifacts.

This package contains utilities for:
- evidence bundle integrity verification (hashes + signatures)
- bundle signing helpers

It is intentionally dependency-light; signing may require optional extras.
"""
