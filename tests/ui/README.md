# UI Helper Tests

This folder contains Python-side UI helper and parity tests.

Browser-based end-to-end tests for the React surface live under:

- `web/tests/`

These tests remain in `tests/ui/` only when they exercise Python helpers,
contracts, or parity utilities used by the UI surface.
