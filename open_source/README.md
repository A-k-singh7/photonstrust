# Open Source Subprojects

This directory contains separately managed public-facing extracts related to
PhotonTrust.

## Current Contents

- `qiskit_protocols_public/`
  - a nested public protocol-focused subproject with its own repository metadata
    and workflow files

## Policy

These folders should be treated as independently versioned subprojects unless a
maintainer explicitly documents shared release mechanics.
