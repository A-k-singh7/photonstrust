# Phase 37 - GDS + KLayout Pack Enablement (v0.1.1) - Build Log (2026-02-14)

## Dependency Enablement

Installed optional dependency required for `.gds` emission:

```powershell
cd c:\Users\aksin\Desktop\Qutip+qskit projects\photonstrust
py -m pip install gdstk
py -c "import gdstk; print(getattr(gdstk, '__version__', None))"
```

Observed:
- `gdstk 0.9.62`

## Code Changes

1. Emit true GDS PATH elements for waveguides:
- `photonstrust/photonstrust/layout/pic/build_layout.py`

2. Normalize gdstk PATH layer/dtype sequences correctly (tuple-safe):
- `photonstrust/photonstrust/layout/gds_extract.py`

3. Default KLayout macro settings from the layout build report (PDK min width + layers + top cell):
- `photonstrust/photonstrust/api/server.py`

## Notes

- No change to the "tool seam" posture:
  - KLayout remains optional (`allow_missing_tool=True` in the artifact-pack wrapper).
  - API runs remain rooted under `PHOTONTRUST_API_RUNS_ROOT`.

