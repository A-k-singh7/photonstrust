# Phase 25 — Build Log — 2026-02-13

## Summary
Implemented a deterministic calibration loop for the parallel-waveguide crosstalk model using a schema-validatable sweep format, plus a drift gate script integrated into the release gate.

## Code Changes

### Added
- `schemas/photonstrust.pic_crosstalk_sweep.v0.schema.json`
- `photonstrust/calibrate/pic_crosstalk.py`
  - load + validate sweep JSON
  - deterministic least-squares fit for `kappa0_per_um`, `gap_decay_um`, `lambda_exp`
  - outputs calibration report with metrics + provenance hash
- `scripts/check_pic_crosstalk_calibration_drift.py`
- Fixtures:
  - `tests/fixtures/measurement_bundle_pic_crosstalk/measurement_bundle.json`
  - `tests/fixtures/measurement_bundle_pic_crosstalk/data/pic_crosstalk_sweep.json`
  - `tests/fixtures/pic_crosstalk_calibration_baseline.json`
- Tests:
  - `tests/test_pic_crosstalk_calibration.py`

### Modified
- `scripts/release_gate_check.py`
  - added `pic_crosstalk_calibration_drift` check
- `tests/test_measurement_ingestion.py`
  - added ingestion coverage for the new crosstalk sweep measurement bundle fixture

## Notes / Decisions
- Fit method is intentionally simple and inspectable: linear least squares on the quadratic-region approximation.
- Defaults in the predictor remain unchanged; calibration outputs parameter sets that can be provided explicitly via API/UI later.

