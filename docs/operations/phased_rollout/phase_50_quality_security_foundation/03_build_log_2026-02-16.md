# Phase 50: Quality + Security Foundation (Build Log)

Date: 2026-02-16

## Implemented

Phase scaffolding and governance lock for Week 1:

- Added new phase folder:
  - `docs/operations/phased_rollout/phase_50_quality_security_foundation/`
- Added mandatory phase artifacts:
  - `01_research_brief_2026-02-16.md`
  - `02_implementation_plan_2026-02-16.md`
  - `03_build_log_2026-02-16.md`
  - `04_validation_report_2026-02-16.md`
- Updated phased rollout index:
  - `docs/operations/phased_rollout/README.md`
- Added weekly operations risk note (refreshed risk register):
  - `docs/operations/week1/phase50_w01_ops_notes_2026-02-16.md`

## Owner map lock outcome

- Release-critical workstreams have explicit accountable and responsible roles.
- No owner gaps remain for Week 1 exit criteria.

## Validation command executed

- `py -3 scripts/release_gate_check.py`

## Week 2 implementation (CI matrix + coverage floor)

- Updated CI workflow:
  - `.github/workflows/ci.yml`
  - Added Python matrix (`3.9`, `3.10`, `3.11`, `3.12`) for the main test job.
  - Added coverage-enforced pytest args in `scripts/ci_checks.py` invocation.
  - Restricted benchmark drift check to the `3.11` lane to avoid duplicate drift runs.
  - Added non-blocking optional dependency lanes with extras install + import smoke:
    - `dev,qutip`
    - `dev,qiskit`
    - `dev,api`
    - `dev,layout`
- Updated coverage policy configuration:
  - `pyproject.toml`
  - Added `pytest-cov` to `dev` extras.
  - Added coverage configuration (`tool.coverage.run`, `tool.coverage.report`) with
    `fail_under = 70`.

## Week 2 validation command executed

- `py -3 -m pytest -q --cov=photonstrust --cov-fail-under=70`

## Week 3 implementation (security baseline)

- Added automated dependency update policy:
  - `.github/dependabot.yml`
  - Configured weekly update lanes for `pip`, `npm` (`/web`), and
    `github-actions`.
- Added security CI workflow:
  - `.github/workflows/security-baseline.yml`
  - Blocking runtime dependency scan with `pip-audit`.
  - Deterministic web dependency install check using `npm ci`.
  - Non-blocking runtime Node audit baseline (`npm audit --omit=dev`).
- Hardened vulnerability disclosure policy:
  - `SECURITY.md`
  - Added response targets, coordinated disclosure protocol, and
    dependency-monitoring policy statements.
- Updated deterministic web install guidance in active docs:
  - `README.md`
  - `web/README.md`

## Week 3 validation command executed

- `pip-audit` (executed in an isolated temporary environment to avoid
  environment-local dependency noise)

## Week 4 implementation (config schema versioning + migration skeleton)

- Added scenario config schema governance:
  - `photonstrust/config.py`
  - Added supported schema-version constants and migration hook registry.
  - Added migration path for legacy/missing schema version (`0.0` -> `0.1`).
  - Added strict fail-fast error for unsupported schema versions with migration
    guidance.
- Added strict CLI failure handling for unsupported config schema versions:
  - `photonstrust/cli.py`
- Added validation coverage for migration and strict-failure behavior:
  - `tests/test_config_schema_versioning.py`
- Seeded explicit schema version in pilot validation config:
  - `configs/pilot_day0_kickoff.yml`
- Seeded explicit schema version in default demo config:
  - `configs/demo1_default.yml`

## Week 4 validation commands executed

- `py -3 -m pytest -q tests/test_config_schema_versioning.py`
- `py -3 -m photonstrust.cli run configs/pilot_day0_kickoff.yml --validate-only`
- `py -3 -m pytest -q`
