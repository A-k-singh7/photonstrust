# Build Log

## Metadata
- Work item ID: PT-PHASE-20
- Date: 2026-02-13
- Scope: Run browser + run diff v0.1 (managed-service hardening, local dev)

## Changes Implemented

### Backend (API)
- Added run diff helper:
  - `photonstrust/api/diff.py`
    - bounded JSON diff
    - JSON Pointer escaping for paths (RFC 6901)
    - list values treated as atomic to keep diffs stable/bounded
- Added API endpoint:
  - `POST /v0/runs/diff` in `photonstrust/api/server.py`
    - default scope: `input` (manifest input blocks)
    - bounded by `limit` (clamped)

Files:
- `photonstrust/api/diff.py`
- `photonstrust/api/server.py`

### Web UI
- Added `Runs` mode:
  - browse runs from `GET /v0/runs`
  - select run to load manifest (`GET /v0/runs/{run_id}`)
  - diff two runs via `POST /v0/runs/diff`
  - open served artifacts from manifest relpaths (`/v0/runs/{run_id}/artifact?path=...`)

Files:
- `web/src/photontrust/api.js`
- `web/src/App.jsx`

### Tests
- Added diff endpoint coverage:
  - create two orbit runs with different inputs
  - assert diff includes `/config_hash` change and respects `limit`

Files:
- `tests/test_api_server_optional.py`

## Notes
- Diff currently targets the manifest `input` blocks, not domain outputs. Domain-aware
  output diffs are deferred until output summary contracts are stabilized.
