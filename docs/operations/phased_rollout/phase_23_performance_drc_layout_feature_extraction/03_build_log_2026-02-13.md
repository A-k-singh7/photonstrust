# Phase 23 — Build Log — 2026-02-13

## Summary
Implemented route-level layout feature extraction (parallel waveguide run segments) and integrated it into the Performance DRC crosstalk check as a worst-case envelope across extracted segments.

## Code Changes

### Added
- `photonstrust/layout/__init__.py`
- `photonstrust/layout/route_extract.py`
- `photonstrust/verification/layout_features.py`
- `tests/test_layout_route_extract.py`

### Modified
- `photonstrust/verification/performance_drc.py`
  - Added optional `routes` + `layout_extract` mode:
    - Extracts parallel runs from routes.
    - Computes worst-case XT envelope across segments and wavelengths.
    - Applies physical min-gap DRC across all extracted segments.
    - Publishes `results.layout` + `provenance.layout_hash`.
  - Scalar (`gap_um`, `parallel_length_um`) mode remains unchanged.
- `photonstrust/reporting/performance_drc_report.py`
  - HTML report now displays optional extracted layout summary.
- `tests/test_performance_drc_schema.py`
  - Added route-mode schema validation case.
- `tests/api/test_api_server_optional.py`
  - Added route-mode API test for `POST /v0/performance_drc/crosstalk`.
- `web/src/App.jsx`
  - Gate fix: actually use existing JSON-apply handlers by adding advanced editors for:
    - `uncertainty`
    - `finite_key`

## Notes / Decisions
- v0.1 extractor supports Manhattan routes only (axis-aligned polyline segments).
- Gap is computed as **edge-to-edge** spacing using route widths (centerline separation minus half-widths).
- In route-mode Performance DRC, `points[*].xt_db` represents the worst XT across all extracted segments at each wavelength.

