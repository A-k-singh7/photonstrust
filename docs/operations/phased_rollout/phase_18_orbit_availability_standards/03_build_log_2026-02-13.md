# Build Log

## Metadata
- Work item ID: PT-PHASE-18
- Title: OrbitVerify evidence hardening v0.2 (availability envelope + standards anchors)
- Date: 2026-02-13

## Summary

Extended OrbitVerify pass envelope outputs with:
- an explicit availability assumption (`clear_fraction`) and expected-keys scaling, and
- standards anchors surfaced in artifacts (as references, not compliance claims).

This improves scientific auditability and makes operational assumptions explicit
in both JSON results and the generated HTML report.

## Code Changes

### Engine (Orbit)
- `photonstrust/photonstrust/orbit/pass_envelope.py`
  - Added optional config parsing for `orbit_pass.availability.clear_fraction`.
  - Extended per-case `summary`:
    - `expected_total_keys_bits`
    - `clear_fraction`
  - Added top-level metadata:
    - `availability` block (model + clear_fraction + explicit flag)
    - `standards_anchors` list
  - Updated HTML report:
    - shows `clear_fraction` in header
    - adds `expected_total_keys_bits` column
    - renders anchors panel

### Diagnostics
- `photonstrust/photonstrust/orbit/diagnostics.py`
  - Added semantic checks for `orbit_pass.availability.clear_fraction` in `[0, 1]`.

### Schema (config contract)
- `photonstrust/schemas/photonstrust.orbit_pass_envelope.v0_1.schema.json`
  - Added an optional `availability.clear_fraction` property definition (backward compatible).

### Tests
- `photonstrust/tests/test_orbit_pass_envelope.py`
  - Added test asserting expected keys scaling under `clear_fraction`.
- `photonstrust/tests/test_orbit_diagnostics.py`
  - Added test asserting diagnostics catch out-of-range `clear_fraction`.

## Notes

- Availability modeling is intentionally minimal and explicitly labeled.
- Anchors are surfaced as references only; no standards compliance is implied.

