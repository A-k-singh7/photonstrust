# Build Log

## Metadata
- Work item ID: PT-PHASE-22
- Date: 2026-02-13
- Scope: Project registry + approvals v0.1 (managed-service governance, local dev)

## Changes Implemented

### Backend (API)
- Added filesystem-backed project store and approvals log (append-only JSONL):
  - `photonstrust/api/projects.py`
    - `GET /v0/projects` is backed by inferred project summaries from run manifests
    - `POST /v0/projects/{project_id}/approvals` appends approval events
    - `GET /v0/projects/{project_id}/approvals` lists approval events
    - strict `project_id` validation (lowercase slug; default `default`)
- Tagged runs with `project_id` and enabled filtering:
  - `GET /v0/runs?project_id=<id>` (validated)
  - run manifests now store `input.project_id` for:
    - `POST /v0/qkd/run`
    - `POST /v0/orbit/pass/run`

Files:
- `photonstrust/api/projects.py` (new)
- `photonstrust/api/runs.py`
- `photonstrust/api/server.py`

### Web UI
- Runs mode now supports:
  - project selector (filter runs by `project_id`)
  - approvals panel on the manifest view (actor + note; submit approval)
  - shows `project_id` on run cards and selectors

Files:
- `web/src/photontrust/api.js`
- `web/src/App.jsx`

### Tests
- Added API optional coverage for project registry + approvals:
  - create a run with `project_id`
  - assert `GET /v0/projects` includes it
  - assert `GET /v0/runs?project_id=...` filters
  - append and retrieve approvals events

Files:
- `tests/test_api_server_optional.py`

## Notes
- Approvals are append-only (JSONL). Deletion/rewrites are intentionally out of scope.
- Approval events record:
  - `run_manifest_hash` and `outputs_summary_hash` (for audit/review)
