# Phase 26 — Build Log — 2026-02-13

## Summary
Upgraded `pic.ring` from a placeholder to a wavelength-dependent all-pass ring resonator transfer function, compatible with existing chain/DAG solvers and sweep workflows.

## Code Changes

### Modified
- `photonstrust/components/pic/library.py`
  - Added `_matrix_ring()` implementing a 2-port all-pass ring transfer when resonator params are provided.
  - Preserved backwards-compatible placeholder behavior when no resonator params are present.
- `photonstrust/registry/kinds.py`
  - Updated `pic.ring` parameters and notes to reflect the v0.2 resonator model.

### Added
- `tests/test_pic_ring_resonance.py`
  - Sweep test verifies the ring response is not flat vs wavelength (resonance notch exists).

## Notes / Decisions
- Ring is modeled as a lumped 2-port response (forward-only), not as an explicit feedback loop network.
- This keeps the solver simple and deterministic while enabling credible ring/filter sweeps immediately.

