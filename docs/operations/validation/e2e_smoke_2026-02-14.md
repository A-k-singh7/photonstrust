# PhotonTrust E2E Smoke Test (2026-02-14)

This document captures real end-to-end executions of the PhotonTrust PIC workflow chain on this machine, including evidence bundle export and replay.

## Environment

- Date: 2026-02-14
- OS: Windows 11 (`Windows-11-10.0.26200-SP0`)
- Python: `3.12.10`
- PhotonTrust package version: `0.1.0`
- KLayout: `C:\Users\aksin\AppData\Roaming\KLayout\klayout_app.exe`
- `gdstk`: `0.9.62`

## Step 1: Python Test Suite

Command:

```powershell
cd c:\Users\aksin\Desktop\Qutip+qskit projects\photonstrust
py -m pytest -q
```

Result:

- `145 passed, 2 skipped`

## Step 2: Release Gate

Command:

```powershell
cd c:\Users\aksin\Desktop\Qutip+qskit projects\photonstrust
py scripts\release_gate_check.py
```

Result:

- `Release gate: PASS`
- Report: `photonstrust/results/release_gate/release_gate_report.json`

## Step 3: Web Lint + Build

Command:

```powershell
cd c:\Users\aksin\Desktop\Qutip+qskit projects\photonstrust\web
npm run lint
npm run build
```

Result:

- `eslint`: PASS
- `vite build`: PASS

## Step 4: E2E Workflow Chain (Historical, Before `.gds` Emission)

Runs root:

- `photonstrust/results/e2e_smoke/2026-02-14_18-53-01`

Chain:

1. Inverse design (`mzi_phase`, `steps=61`, `target_power_fraction=0.9`)
2. Layout build (ports/routes + provenance; `.gds` optional)
3. LVS-lite (expected vs extracted connectivity)
4. Optional KLayout pack (skipped in this historical run because no `.gds`)
5. SPICE export (netlist + mapping + provenance)

Root workflow run:

- Workflow run ID: `93c8d1b0b65b`
- Status: `pass`
- Run dir: `photonstrust/results/e2e_smoke/2026-02-14_18-53-01/run_93c8d1b0b65b`

Child runs:

- invdesign: `a31c2cf14171`
- layout_build: `a025d965e833`
- lvs_lite: `a39a49c6cb36` (pass = `true`)
- spice_export: `4e106f7aa7ad`
- klayout_pack: `skipped` (`layout did not emit a .gds artifact`)

Evidence bundle (includes children):

- `photonstrust/results/e2e_smoke/2026-02-14_18-53-01/run_93c8d1b0b65b/evidence_bundle_with_children.zip`

Replay run:

- Replay workflow run ID: `d903dbbcc3c4`
- Status: `pass`
- Run dir: `photonstrust/results/e2e_smoke/2026-02-14_18-53-01/run_d903dbbcc3c4`
- Evidence bundle (includes children): `photonstrust/results/e2e_smoke/2026-02-14_18-53-01/run_d903dbbcc3c4/evidence_bundle_with_children.zip`

## Step 5: E2E Workflow Chain (With `.gds` + KLayout Pack)

### Install `gdstk`

Command:

```powershell
cd c:\Users\aksin\Desktop\Qutip+qskit projects\photonstrust
py -m pip install gdstk
py -c "import gdstk; print(getattr(gdstk, '__version__', None))"
```

### Implementation Notes

- Layout `.gds` emission uses true GDS PATH elements (not polygonized boundaries) so KLayout can extract route spines:
  - `photonstrust/photonstrust/layout/pic/build_layout.py`
- KLayout DRC-lite settings default from the source layout build report (PDK min width + layer conventions + top cell):
  - `photonstrust/photonstrust/api/server.py`

### Verified Full Chain Run

Runs root:

- `photonstrust/results/e2e_smoke/2026-02-14_19-10-27`

Root workflow run:

- Workflow run ID: `3f62892d1c34`
- Status: `pass`
- Run dir: `photonstrust/results/e2e_smoke/2026-02-14_19-10-27/run_3f62892d1c34`
- Evidence bundle (includes children): `photonstrust/results/e2e_smoke/2026-02-14_19-10-27/run_3f62892d1c34/evidence_bundle_with_children.zip`

Child runs:

- invdesign: `a8c3e303661a`
- layout_build: `4117a61d1e8c`
- lvs_lite: `2cf35df36226` (pass = `true`)
- klayout_pack: `8c06baa07534` (status = `pass`, DRC-lite status = `pass`, `min_waveguide_width_um = 0.45`)
- spice_export: `586c43db5326`

